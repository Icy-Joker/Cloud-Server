apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: tailscale-cluster-role-binding
subjects:
  - kind: ServiceAccount
    name: tailscale
    namespace: default
roleRef:
  kind: ClusterRole
  name: tailscale-cluster-role
  apiGroup: rbac.authorization.k8s.io
