apiVersion: v1
kind: Service
metadata:
  name: kubernetes-template
  namespace: default
  labels:
    app: kubernetes-template
spec:
  type: NodePort
  selector:
    app: kubernetes-template
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
