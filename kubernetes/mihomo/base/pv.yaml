apiVersion: v1
kind: PersistentVolume
metadata:
  name: mihomo
  labels:
    app.kubernetes.io/name: mihomo
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: mihomo-storage
  volumeMode: Filesystem
  local:
    path: /srv/mihomo
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - cloud-server
