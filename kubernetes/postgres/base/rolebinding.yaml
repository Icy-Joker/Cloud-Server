apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: postgres-role-binding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: postgres
    namespace: default
roleRef:
  kind: Role
  name: postgres-role
  apiGroup: rbac.authorization.k8s.io
