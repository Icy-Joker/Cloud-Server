apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: registry-cluster-role-binding
subjects:
  - kind: ServiceAccount
    name: registry
    namespace: default
roleRef:
  kind: ClusterRole
  name: registry-cluster-role
  apiGroup: rbac.authorization.k8s.io
