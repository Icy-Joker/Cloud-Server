apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: registry-role-binding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: registry
    namespace: default
roleRef:
  kind: Role
  name: registry-role
  apiGroup: rbac.authorization.k8s.io
