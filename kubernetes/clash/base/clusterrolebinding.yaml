apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: clash-cluster-role-binding
subjects:
  - kind: ServiceAccount
    name: clash
    namespace: default
roleRef:
  kind: ClusterRole
  name: clash-cluster-role
  apiGroup: rbac.authorization.k8s.io
